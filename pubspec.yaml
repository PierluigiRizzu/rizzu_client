name: rizzu_client
description: Networking Request Builder, using Dio. Its purpose is to make easier doing API requests and to give devs an out-of-the box working client.
version: 0.1.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.0
    

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

